= error_messages_for @folder
= form_for @folder, :html => { :class => "formarea" } do |f|
  %div
    = f.label :name
    = f.text_field :name
  %div
    %label.padding &nbsp;
    = f.submit "Update Folder"